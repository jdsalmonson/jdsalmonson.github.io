# frozen_string_literal: true

# To test this webpage:  (-JDS 1/21/2025)
# > cd jdsalmonson.github.io
# > bundle exec jekyll serve
# It will then report the web address to which a browser can be pointed.


source "https://rubygems.org"

gem "jekyll", "~> 4.3" # Adjust the version if necessary
gem "webrick"          # Required for Jekyll in Ruby 3.x
gem "jekyll-sitemap"
gem "jekyll-feed"
gem "jekyll-seo-tag"
gem "jekyll-paginate"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
